@AGENTS.md

## Notas específicas de Claude Code
- Ejecutar `pnpm type-check && pnpm lint && pnpm test` antes de proponer un commit.
- No modificar archivos bajo `db/migrations/` ya aplicados; crear una migración nueva.